language: r
cache: packages
warnings_are_errors: true

sudo: required

before_install:
  Rscript -e 'update.packages(ask = FALSE)'

r_packages:
  - rgeos
  - sp
  - covr
  
r_binary_packages:
  - rgdal

notifications:
  email:
    on_success: change
    on_failure: change